# 01 — Property Access

## Expression
```
$.metadata.version
```

## Explanation
- `$` — the root of the input document
- `.metadata` — navigate into the `metadata` object
- `.version` — access the `version` property

This is the simplest form of Elwood expression: a JSONPath that navigates into nested objects using dot notation.
